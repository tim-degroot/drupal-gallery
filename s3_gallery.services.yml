services:
  aws.s3_client:
    class: Aws\S3\S3Client
    factory: ['Aws\S3\S3Client', 'factory']
    arguments:
      - 
        version: 'latest'
        region: 'eu-central-1'
        credentials: 
          key: '%aws_s3.key%'
          secret: '%aws_s3.secret%'